#ifndef SCRIPTABLE_CLASS_COMPONENT_H
#define SCRIPTABLE_CLASS_COMPONENT_H

#include <string>

struct ScriptableClassComponent {
    std::string classPath;
    std::string className;
};

#endif //SCRIPTABLE_CLASS_COMPONENT_H
